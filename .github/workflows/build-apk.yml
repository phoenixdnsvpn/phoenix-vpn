name: Build VayDNS Android APK (Multi-Arch)

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.8'

      - name: Set up Android SDK & NDK
        uses: android-actions/setup-android@v3

      - name: Install NDK
        run: |
          echo "y" | sdkmanager "ndk;27.0.12077973" 
          echo "ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/27.0.12077973" >> $GITHUB_ENV

      - name: Install Go Mobile Tools
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          $(go env GOPATH)/bin/gomobile init

      - name: Clean Previous Builds
        working-directory: ./android
        run: chmod +x gradlew && ./gradlew clean

      - name: Build ARM64 Architecture
        run: |
          # 1. Build ARM64 AAR (Using the exact name Gradle expects)
          cd mobile
          go mod tidy
          mkdir -p ../android/app/libs
          $(go env GOPATH)/bin/gomobile bind -v \
            -target=android/arm64 \
            -androidapi 24 \
            -ldflags="-s -w" \
            -trimpath \
            -o ../android/app/libs/vaydns-arm64.aar .
          
          # 2. Build ARM64 APK
          cd ../android
          ./gradlew assembleDebug -Pandroid.injected.abi=arm64-v8a
          
          # 3. Save artifacts
          mkdir -p ../release-artifacts
          TAG_NAME=${GITHUB_REF#refs/tags/}
          cp app/build/outputs/apk/debug/app-debug.apk ../release-artifacts/VaydnsVpn-${TAG_NAME}-arm64-v8a.apk
          cp app/libs/vaydns-arm64.aar ../release-artifacts/vaydns-arm64.aar
          
          # Cleanup AAR to prevent conflicts in the next step
          rm app/libs/vaydns-arm64.aar

      - name: Build ARMv7 Architecture
        run: |
          # 1. Build ARMv7 AAR (Using the name Gradle needs for 32-bit pass)
          cd mobile
          mkdir -p ../android/app/libs
          $(go env GOPATH)/bin/gomobile bind -v \
            -target=android/arm \
            -androidapi 24 \
            -ldflags="-s -w" \
            -trimpath \
            -o ../android/app/libs/vaydns-armeabi.aar .
          
          # 2. Build ARMv7 APK
          cd ../android
          # We run clean to ensure the 64-bit cache doesn't interfere
          ./gradlew clean
          ./gradlew assembleDebug -Pandroid.injected.abi=armeabi-v7a
          
          # 3. Save artifacts
          TAG_NAME=${GITHUB_REF#refs/tags/}
          cp app/build/outputs/apk/debug/app-debug.apk ../release-artifacts/VaydnsVpn-${TAG_NAME}-armeabi-v7a.apk
          cp app/libs/vaydns-armeabi.aar ../release-artifacts/vaydns-armeabi.aar

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            release-artifacts/VaydnsVpn-*.apk
            release-artifacts/vaydns-arm64.aar
            release-artifacts/vaydns-armeabi.aar
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
